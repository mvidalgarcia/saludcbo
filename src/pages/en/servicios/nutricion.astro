---
import ServicePage from '../../../components/ServicePage.astro';
import { useTranslations } from '../../../i18n/utils';
import type { Lang } from '../../../i18n/utils';
import { getCollection } from 'astro:content';

const lang = (Astro.currentLocale ?? 'en') as Lang;
const t = useTranslations(lang);

const services = await getCollection('services');
const svc = services.find(s => s.id === 'nutricion')!;
const tr = svc.data.translations[lang];
---

<ServicePage
  lang={lang}
  serviceId="nutricion"
  metaTitle={t('meta.nutricion.title')}
  metaDescription={t('meta.nutricion.description')}
  heroEyebrow={t('services.nutricion.hero.eyebrow')}
  heroTitle={t('services.nutricion.hero.title')}
  heroSubtitle={t('services.nutricion.hero.subtitle')}
  description={tr.description}
  tagline={tr.tagline}
  features={tr.features}
  process={tr.process}
  types={tr.types}
/>
